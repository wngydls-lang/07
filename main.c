#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
  int flag = 0;
  int y;
  while(flag != 0)
  {
    y = 3;
    flag = 0;
  }
  y = 4;
  
  printf("flag: %d, y: %d\n", flag, y);
  
  system("PAUSE");	
  return 0;
}
